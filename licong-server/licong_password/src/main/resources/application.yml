server:
  port: 9008
spring:
  application:
    name: licong-password #指定服务名
  datasource:
    driverClassName: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/licong_password?characterEncoding=UTF8&useSSL=false
    username: root
    password: asd6656
    jpa:
      database: MySQL
      show‐sql: true
      generate‐ddl: true
eureka:
  client:
    service‐url:
      defaultZone: http://localhost:6868/eureka
  instance:
    prefer‐ip‐address: true
jwt:
  config:
    key: licong
    ttl: 2592000000